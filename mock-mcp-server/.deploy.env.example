# Local deploy script config — copy to .deploy.env (gitignored).
# Used by ./deploy.sh

DOCKER_USER=mrbo0911
VPS_HOST=103.253.145.163
VPS_USER=root

# Override only if VPS uses a non-standard path.
# DEPLOY_PATH=/opt/aireno-mock-mcp
